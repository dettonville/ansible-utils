---

test_host_list:
  - hostname: vmlnx123-q1-s1.example.int
    hostvars:
      provisioning_data:
        jira_id: DCC-12345
        infra_group: MIDWA
    groups:
    - ntp_client
    - ldap_client
  - hostname: vmlnx124-q1-s1.example.int
    hostvars:
      provisioning_data:
        jira_id: DCC-12346
        infra_group: MIDWA
    groups:
    - ntp_client
    - nfs_client
    - ldap_client
